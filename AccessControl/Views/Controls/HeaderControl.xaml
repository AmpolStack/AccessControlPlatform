<UserControl x:Class="AccessControl.Views.Controls.HeaderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AccessControl.Views.Controls"
             mc:Ignorable="d" 
             x:Name="ucHeader"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MaxWidth="200"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*" MaxWidth="400"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>


        <TextBlock Grid.Column="0"
          Grid.Row="0"
          FontWeight="{StaticResource InterBoldWeight}"
          FontSize="32"
          Text="CONTROL&#10;DE&#10;INGRESO"
          LineHeight="24"
          LineStackingStrategy="BlockLineHeight"
          TextAlignment="Left">
        </TextBlock>

        <TextBlock Grid.Column="2"
                  Grid.Row="0"
                  FontWeight="{StaticResource InterSemiBoldWeight}"
                  Foreground="{StaticResource AppBlueBrush}"
                  FontSize="30"
                  Text="{Binding UserName, ElementName=ucHeader}"
                  LineHeight="24"
                  LineStackingStrategy="BlockLineHeight"
                  TextAlignment="Right">
        </TextBlock>

    </Grid>
</UserControl>
